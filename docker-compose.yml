version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8004:8000"
    volumes:
      - ./app/persistant:/app/persistant
    environment:
      - ALLOWED_ORIGINS=["http://localhost:8004"]
